# Used by docker compose (variable substitution). Copy to .env:  cp .env.example .env
ANTHROPIC_API_KEY=
# Host port the app is published on (default 8080; set 80 on a server for a clean URL)
WEB_PORT=8080
